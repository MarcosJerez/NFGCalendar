<?php

/**
 * Base class that represents a row from the 'sft_usu_atributos' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Mon Sep 24 21:13:16 2012
 *
 * @package    plugins.sftGuardPlugin.lib.model.om
 */
abstract class BaseSftUsuAtributo extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        SftUsuAtributoPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the nombre field.
	 * @var        string
	 */
	protected $nombre;

	/**
	 * The value for the descripcion field.
	 * @var        string
	 */
	protected $descripcion;

	/**
	 * The value for the formato field.
	 * @var        string
	 */
	protected $formato;

	/**
	 * The value for the relevancia field.
	 * @var        string
	 */
	protected $relevancia;

	/**
	 * The value for the origen field.
	 * @var        string
	 */
	protected $origen;

	/**
	 * The value for the oid field.
	 * @var        string
	 */
	protected $oid;

	/**
	 * The value for the urn field.
	 * @var        string
	 */
	protected $urn;

	/**
	 * The value for the sintaxis_ldap field.
	 * @var        string
	 */
	protected $sintaxis_ldap;

	/**
	 * The value for the indexado field.
	 * @var        string
	 */
	protected $indexado;

	/**
	 * The value for the multivaluado field.
	 * @var        int
	 */
	protected $multivaluado;

	/**
	 * The value for the ejemplo field.
	 * @var        string
	 */
	protected $ejemplo;

	/**
	 * The value for the notas field.
	 * @var        string
	 */
	protected $notas;

	/**
	 * @var        array SftUsuAtributoValor[] Collection to store aggregation of SftUsuAtributoValor objects.
	 */
	protected $collSftUsuAtributoValors;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftUsuAtributoValors.
	 */
	private $lastSftUsuAtributoValorCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'SftUsuAtributoPeer';

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [nombre] column value.
	 * 
	 * @return     string
	 */
	public function getNombre()
	{
		return $this->nombre;
	}

	/**
	 * Get the [descripcion] column value.
	 * 
	 * @return     string
	 */
	public function getDescripcion()
	{
		return $this->descripcion;
	}

	/**
	 * Get the [formato] column value.
	 * 
	 * @return     string
	 */
	public function getFormato()
	{
		return $this->formato;
	}

	/**
	 * Get the [relevancia] column value.
	 * 
	 * @return     string
	 */
	public function getRelevancia()
	{
		return $this->relevancia;
	}

	/**
	 * Get the [origen] column value.
	 * 
	 * @return     string
	 */
	public function getOrigen()
	{
		return $this->origen;
	}

	/**
	 * Get the [oid] column value.
	 * 
	 * @return     string
	 */
	public function getOid()
	{
		return $this->oid;
	}

	/**
	 * Get the [urn] column value.
	 * 
	 * @return     string
	 */
	public function getUrn()
	{
		return $this->urn;
	}

	/**
	 * Get the [sintaxis_ldap] column value.
	 * 
	 * @return     string
	 */
	public function getSintaxisLdap()
	{
		return $this->sintaxis_ldap;
	}

	/**
	 * Get the [indexado] column value.
	 * 
	 * @return     string
	 */
	public function getIndexado()
	{
		return $this->indexado;
	}

	/**
	 * Get the [multivaluado] column value.
	 * 
	 * @return     int
	 */
	public function getMultivaluado()
	{
		return $this->multivaluado;
	}

	/**
	 * Get the [ejemplo] column value.
	 * 
	 * @return     string
	 */
	public function getEjemplo()
	{
		return $this->ejemplo;
	}

	/**
	 * Get the [notas] column value.
	 * 
	 * @return     string
	 */
	public function getNotas()
	{
		return $this->notas;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [nombre] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nombre !== $v) {
			$this->nombre = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::NOMBRE;
		}

		return $this;
	} // setNombre()

	/**
	 * Set the value of [descripcion] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setDescripcion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->descripcion !== $v) {
			$this->descripcion = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::DESCRIPCION;
		}

		return $this;
	} // setDescripcion()

	/**
	 * Set the value of [formato] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setFormato($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->formato !== $v) {
			$this->formato = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::FORMATO;
		}

		return $this;
	} // setFormato()

	/**
	 * Set the value of [relevancia] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setRelevancia($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->relevancia !== $v) {
			$this->relevancia = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::RELEVANCIA;
		}

		return $this;
	} // setRelevancia()

	/**
	 * Set the value of [origen] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setOrigen($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->origen !== $v) {
			$this->origen = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::ORIGEN;
		}

		return $this;
	} // setOrigen()

	/**
	 * Set the value of [oid] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setOid($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->oid !== $v) {
			$this->oid = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::OID;
		}

		return $this;
	} // setOid()

	/**
	 * Set the value of [urn] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setUrn($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->urn !== $v) {
			$this->urn = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::URN;
		}

		return $this;
	} // setUrn()

	/**
	 * Set the value of [sintaxis_ldap] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setSintaxisLdap($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->sintaxis_ldap !== $v) {
			$this->sintaxis_ldap = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::SINTAXIS_LDAP;
		}

		return $this;
	} // setSintaxisLdap()

	/**
	 * Set the value of [indexado] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setIndexado($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->indexado !== $v) {
			$this->indexado = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::INDEXADO;
		}

		return $this;
	} // setIndexado()

	/**
	 * Set the value of [multivaluado] column.
	 * 
	 * @param      int $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setMultivaluado($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->multivaluado !== $v) {
			$this->multivaluado = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::MULTIVALUADO;
		}

		return $this;
	} // setMultivaluado()

	/**
	 * Set the value of [ejemplo] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setEjemplo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ejemplo !== $v) {
			$this->ejemplo = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::EJEMPLO;
		}

		return $this;
	} // setEjemplo()

	/**
	 * Set the value of [notas] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuAtributo The current object (for fluent API support)
	 */
	public function setNotas($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->notas !== $v) {
			$this->notas = $v;
			$this->modifiedColumns[] = SftUsuAtributoPeer::NOTAS;
		}

		return $this;
	} // setNotas()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->nombre = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->descripcion = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->formato = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->relevancia = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->origen = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->oid = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->urn = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->sintaxis_ldap = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->indexado = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->multivaluado = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->ejemplo = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->notas = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 13; // 13 = SftUsuAtributoPeer::NUM_COLUMNS - SftUsuAtributoPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating SftUsuAtributo object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftUsuAtributoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = SftUsuAtributoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collSftUsuAtributoValors = null;
			$this->lastSftUsuAtributoValorCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftUsuAtributoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSftUsuAtributo:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				SftUsuAtributoPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSftUsuAtributo:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftUsuAtributoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSftUsuAtributo:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSftUsuAtributo:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				SftUsuAtributoPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = SftUsuAtributoPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = SftUsuAtributoPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += SftUsuAtributoPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collSftUsuAtributoValors !== null) {
				foreach ($this->collSftUsuAtributoValors as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = SftUsuAtributoPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collSftUsuAtributoValors !== null) {
					foreach ($this->collSftUsuAtributoValors as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SftUsuAtributoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getNombre();
				break;
			case 2:
				return $this->getDescripcion();
				break;
			case 3:
				return $this->getFormato();
				break;
			case 4:
				return $this->getRelevancia();
				break;
			case 5:
				return $this->getOrigen();
				break;
			case 6:
				return $this->getOid();
				break;
			case 7:
				return $this->getUrn();
				break;
			case 8:
				return $this->getSintaxisLdap();
				break;
			case 9:
				return $this->getIndexado();
				break;
			case 10:
				return $this->getMultivaluado();
				break;
			case 11:
				return $this->getEjemplo();
				break;
			case 12:
				return $this->getNotas();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = SftUsuAtributoPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getNombre(),
			$keys[2] => $this->getDescripcion(),
			$keys[3] => $this->getFormato(),
			$keys[4] => $this->getRelevancia(),
			$keys[5] => $this->getOrigen(),
			$keys[6] => $this->getOid(),
			$keys[7] => $this->getUrn(),
			$keys[8] => $this->getSintaxisLdap(),
			$keys[9] => $this->getIndexado(),
			$keys[10] => $this->getMultivaluado(),
			$keys[11] => $this->getEjemplo(),
			$keys[12] => $this->getNotas(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SftUsuAtributoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setNombre($value);
				break;
			case 2:
				$this->setDescripcion($value);
				break;
			case 3:
				$this->setFormato($value);
				break;
			case 4:
				$this->setRelevancia($value);
				break;
			case 5:
				$this->setOrigen($value);
				break;
			case 6:
				$this->setOid($value);
				break;
			case 7:
				$this->setUrn($value);
				break;
			case 8:
				$this->setSintaxisLdap($value);
				break;
			case 9:
				$this->setIndexado($value);
				break;
			case 10:
				$this->setMultivaluado($value);
				break;
			case 11:
				$this->setEjemplo($value);
				break;
			case 12:
				$this->setNotas($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = SftUsuAtributoPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNombre($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setDescripcion($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setFormato($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setRelevancia($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setOrigen($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setOid($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setUrn($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setSintaxisLdap($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setIndexado($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setMultivaluado($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setEjemplo($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setNotas($arr[$keys[12]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(SftUsuAtributoPeer::DATABASE_NAME);

		if ($this->isColumnModified(SftUsuAtributoPeer::ID)) $criteria->add(SftUsuAtributoPeer::ID, $this->id);
		if ($this->isColumnModified(SftUsuAtributoPeer::NOMBRE)) $criteria->add(SftUsuAtributoPeer::NOMBRE, $this->nombre);
		if ($this->isColumnModified(SftUsuAtributoPeer::DESCRIPCION)) $criteria->add(SftUsuAtributoPeer::DESCRIPCION, $this->descripcion);
		if ($this->isColumnModified(SftUsuAtributoPeer::FORMATO)) $criteria->add(SftUsuAtributoPeer::FORMATO, $this->formato);
		if ($this->isColumnModified(SftUsuAtributoPeer::RELEVANCIA)) $criteria->add(SftUsuAtributoPeer::RELEVANCIA, $this->relevancia);
		if ($this->isColumnModified(SftUsuAtributoPeer::ORIGEN)) $criteria->add(SftUsuAtributoPeer::ORIGEN, $this->origen);
		if ($this->isColumnModified(SftUsuAtributoPeer::OID)) $criteria->add(SftUsuAtributoPeer::OID, $this->oid);
		if ($this->isColumnModified(SftUsuAtributoPeer::URN)) $criteria->add(SftUsuAtributoPeer::URN, $this->urn);
		if ($this->isColumnModified(SftUsuAtributoPeer::SINTAXIS_LDAP)) $criteria->add(SftUsuAtributoPeer::SINTAXIS_LDAP, $this->sintaxis_ldap);
		if ($this->isColumnModified(SftUsuAtributoPeer::INDEXADO)) $criteria->add(SftUsuAtributoPeer::INDEXADO, $this->indexado);
		if ($this->isColumnModified(SftUsuAtributoPeer::MULTIVALUADO)) $criteria->add(SftUsuAtributoPeer::MULTIVALUADO, $this->multivaluado);
		if ($this->isColumnModified(SftUsuAtributoPeer::EJEMPLO)) $criteria->add(SftUsuAtributoPeer::EJEMPLO, $this->ejemplo);
		if ($this->isColumnModified(SftUsuAtributoPeer::NOTAS)) $criteria->add(SftUsuAtributoPeer::NOTAS, $this->notas);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(SftUsuAtributoPeer::DATABASE_NAME);

		$criteria->add(SftUsuAtributoPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of SftUsuAtributo (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setNombre($this->nombre);

		$copyObj->setDescripcion($this->descripcion);

		$copyObj->setFormato($this->formato);

		$copyObj->setRelevancia($this->relevancia);

		$copyObj->setOrigen($this->origen);

		$copyObj->setOid($this->oid);

		$copyObj->setUrn($this->urn);

		$copyObj->setSintaxisLdap($this->sintaxis_ldap);

		$copyObj->setIndexado($this->indexado);

		$copyObj->setMultivaluado($this->multivaluado);

		$copyObj->setEjemplo($this->ejemplo);

		$copyObj->setNotas($this->notas);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getSftUsuAtributoValors() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftUsuAtributoValor($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     SftUsuAtributo Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     SftUsuAtributoPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new SftUsuAtributoPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collSftUsuAtributoValors collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftUsuAtributoValors()
	 */
	public function clearSftUsuAtributoValors()
	{
		$this->collSftUsuAtributoValors = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftUsuAtributoValors collection (array).
	 *
	 * By default this just sets the collSftUsuAtributoValors collection to an empty array (like clearcollSftUsuAtributoValors());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftUsuAtributoValors()
	{
		$this->collSftUsuAtributoValors = array();
	}

	/**
	 * Gets an array of SftUsuAtributoValor objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuAtributo has previously been saved, it will retrieve
	 * related SftUsuAtributoValors from storage. If this SftUsuAtributo is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftUsuAtributoValor[]
	 * @throws     PropelException
	 */
	public function getSftUsuAtributoValors($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuAtributoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftUsuAtributoValors === null) {
			if ($this->isNew()) {
			   $this->collSftUsuAtributoValors = array();
			} else {

				$criteria->add(SftUsuAtributoValorPeer::ID_USU_ATRIBUTO, $this->id);

				SftUsuAtributoValorPeer::addSelectColumns($criteria);
				$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftUsuAtributoValorPeer::ID_USU_ATRIBUTO, $this->id);

				SftUsuAtributoValorPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftUsuAtributoValorCriteria) || !$this->lastSftUsuAtributoValorCriteria->equals($criteria)) {
					$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftUsuAtributoValorCriteria = $criteria;
		return $this->collSftUsuAtributoValors;
	}

	/**
	 * Returns the number of related SftUsuAtributoValor objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftUsuAtributoValor objects.
	 * @throws     PropelException
	 */
	public function countSftUsuAtributoValors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuAtributoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftUsuAtributoValors === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftUsuAtributoValorPeer::ID_USU_ATRIBUTO, $this->id);

				$count = SftUsuAtributoValorPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftUsuAtributoValorPeer::ID_USU_ATRIBUTO, $this->id);

				if (!isset($this->lastSftUsuAtributoValorCriteria) || !$this->lastSftUsuAtributoValorCriteria->equals($criteria)) {
					$count = SftUsuAtributoValorPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftUsuAtributoValors);
				}
			} else {
				$count = count($this->collSftUsuAtributoValors);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftUsuAtributoValor object to this object
	 * through the SftUsuAtributoValor foreign key attribute.
	 *
	 * @param      SftUsuAtributoValor $l SftUsuAtributoValor
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftUsuAtributoValor(SftUsuAtributoValor $l)
	{
		if ($this->collSftUsuAtributoValors === null) {
			$this->initSftUsuAtributoValors();
		}
		if (!in_array($l, $this->collSftUsuAtributoValors, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftUsuAtributoValors, $l);
			$l->setSftUsuAtributo($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuAtributo is new, it will return
	 * an empty collection; or if this SftUsuAtributo has previously
	 * been saved, it will retrieve related SftUsuAtributoValors from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuAtributo.
	 */
	public function getSftUsuAtributoValorsJoinSftUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuAtributoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftUsuAtributoValors === null) {
			if ($this->isNew()) {
				$this->collSftUsuAtributoValors = array();
			} else {

				$criteria->add(SftUsuAtributoValorPeer::ID_USU_ATRIBUTO, $this->id);

				$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelectJoinSftUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftUsuAtributoValorPeer::ID_USU_ATRIBUTO, $this->id);

			if (!isset($this->lastSftUsuAtributoValorCriteria) || !$this->lastSftUsuAtributoValorCriteria->equals($criteria)) {
				$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelectJoinSftUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftUsuAtributoValorCriteria = $criteria;

		return $this->collSftUsuAtributoValors;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collSftUsuAtributoValors) {
				foreach ((array) $this->collSftUsuAtributoValors as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collSftUsuAtributoValors = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseSftUsuAtributo:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseSftUsuAtributo::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseSftUsuAtributo
