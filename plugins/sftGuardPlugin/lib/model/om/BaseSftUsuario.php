<?php

/**
 * Base class that represents a row from the 'sft_usuarios' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Mon Sep 24 21:13:16 2012
 *
 * @package    plugins.sftGuardPlugin.lib.model.om
 */
abstract class BaseSftUsuario extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        SftUsuarioPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the id_sfuser field.
	 * @var        int
	 */
	protected $id_sfuser;

	/**
	 * The value for the alias field.
	 * @var        string
	 */
	protected $alias;

	/**
	 * The value for the activo field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $activo;

	/**
	 * The value for the fecha_alta field.
	 * @var        string
	 */
	protected $fecha_alta;

	/**
	 * The value for the fecha_baja field.
	 * @var        string
	 */
	protected $fecha_baja;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * The value for the id_culturapref field.
	 * @var        string
	 */
	protected $id_culturapref;

	/**
	 * The value for the id_persona field.
	 * @var        int
	 */
	protected $id_persona;

	/**
	 * The value for the id_organismo field.
	 * @var        int
	 */
	protected $id_organismo;

	/**
	 * The value for the idp field.
	 * @var        string
	 */
	protected $idp;

	/**
	 * @var        SftPersona
	 */
	protected $aSftPersona;

	/**
	 * @var        SftOrganismo
	 */
	protected $aSftOrganismo;

	/**
	 * @var        array SftAcceso[] Collection to store aggregation of SftAcceso objects.
	 */
	protected $collSftAccesos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftAccesos.
	 */
	private $lastSftAccesoCriteria = null;

	/**
	 * @var        array SftConfPersonal[] Collection to store aggregation of SftConfPersonal objects.
	 */
	protected $collSftConfPersonals;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftConfPersonals.
	 */
	private $lastSftConfPersonalCriteria = null;

	/**
	 * @var        SftControlAcceso one-to-one related SftControlAcceso object
	 */
	protected $singleSftControlAcceso;

	/**
	 * @var        array SftDireccion[] Collection to store aggregation of SftDireccion objects.
	 */
	protected $collSftDireccions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftDireccions.
	 */
	private $lastSftDireccionCriteria = null;

	/**
	 * @var        array SftEstadisticaAplicacion[] Collection to store aggregation of SftEstadisticaAplicacion objects.
	 */
	protected $collSftEstadisticaAplicacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftEstadisticaAplicacions.
	 */
	private $lastSftEstadisticaAplicacionCriteria = null;

	/**
	 * @var        array SftEmail[] Collection to store aggregation of SftEmail objects.
	 */
	protected $collSftEmails;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftEmails.
	 */
	private $lastSftEmailCriteria = null;

	/**
	 * @var        array SftTelefono[] Collection to store aggregation of SftTelefono objects.
	 */
	protected $collSftTelefonos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftTelefonos.
	 */
	private $lastSftTelefonoCriteria = null;

	/**
	 * @var        array SftUsuAtributoValor[] Collection to store aggregation of SftUsuAtributoValor objects.
	 */
	protected $collSftUsuAtributoValors;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftUsuAtributoValors.
	 */
	private $lastSftUsuAtributoValorCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'SftUsuarioPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->activo = true;
	}

	/**
	 * Initializes internal state of BaseSftUsuario object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [id_sfuser] column value.
	 * 
	 * @return     int
	 */
	public function getIdSfuser()
	{
		return $this->id_sfuser;
	}

	/**
	 * Get the [alias] column value.
	 * 
	 * @return     string
	 */
	public function getAlias()
	{
		return $this->alias;
	}

	/**
	 * Get the [activo] column value.
	 * 
	 * @return     boolean
	 */
	public function getActivo()
	{
		return $this->activo;
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_alta] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaAlta($format = 'Y-m-d')
	{
		if ($this->fecha_alta === null) {
			return null;
		}


		if ($this->fecha_alta === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_alta);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_alta, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_baja] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaBaja($format = 'Y-m-d')
	{
		if ($this->fecha_baja === null) {
			return null;
		}


		if ($this->fecha_baja === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_baja);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_baja, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [id_culturapref] column value.
	 * 
	 * @return     string
	 */
	public function getIdCulturapref()
	{
		return $this->id_culturapref;
	}

	/**
	 * Get the [id_persona] column value.
	 * 
	 * @return     int
	 */
	public function getIdPersona()
	{
		return $this->id_persona;
	}

	/**
	 * Get the [id_organismo] column value.
	 * 
	 * @return     int
	 */
	public function getIdOrganismo()
	{
		return $this->id_organismo;
	}

	/**
	 * Get the [idp] column value.
	 * 
	 * @return     string
	 */
	public function getIdp()
	{
		return $this->idp;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [id_sfuser] column.
	 * 
	 * @param      int $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setIdSfuser($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_sfuser !== $v) {
			$this->id_sfuser = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ID_SFUSER;
		}

		return $this;
	} // setIdSfuser()

	/**
	 * Set the value of [alias] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setAlias($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->alias !== $v) {
			$this->alias = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ALIAS;
		}

		return $this;
	} // setAlias()

	/**
	 * Set the value of [activo] column.
	 * 
	 * @param      boolean $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setActivo($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->activo !== $v || $this->isNew()) {
			$this->activo = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ACTIVO;
		}

		return $this;
	} // setActivo()

	/**
	 * Sets the value of [fecha_alta] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setFechaAlta($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_alta !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_alta !== null && $tmpDt = new DateTime($this->fecha_alta)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fecha_alta = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = SftUsuarioPeer::FECHA_ALTA;
			}
		} // if either are not null

		return $this;
	} // setFechaAlta()

	/**
	 * Sets the value of [fecha_baja] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setFechaBaja($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_baja !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_baja !== null && $tmpDt = new DateTime($this->fecha_baja)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fecha_baja = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = SftUsuarioPeer::FECHA_BAJA;
			}
		} // if either are not null

		return $this;
	} // setFechaBaja()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = SftUsuarioPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = SftUsuarioPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Set the value of [id_culturapref] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setIdCulturapref($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->id_culturapref !== $v) {
			$this->id_culturapref = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ID_CULTURAPREF;
		}

		return $this;
	} // setIdCulturapref()

	/**
	 * Set the value of [id_persona] column.
	 * 
	 * @param      int $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setIdPersona($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_persona !== $v) {
			$this->id_persona = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ID_PERSONA;
		}

		if ($this->aSftPersona !== null && $this->aSftPersona->getId() !== $v) {
			$this->aSftPersona = null;
		}

		return $this;
	} // setIdPersona()

	/**
	 * Set the value of [id_organismo] column.
	 * 
	 * @param      int $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setIdOrganismo($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_organismo !== $v) {
			$this->id_organismo = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::ID_ORGANISMO;
		}

		if ($this->aSftOrganismo !== null && $this->aSftOrganismo->getId() !== $v) {
			$this->aSftOrganismo = null;
		}

		return $this;
	} // setIdOrganismo()

	/**
	 * Set the value of [idp] column.
	 * 
	 * @param      string $v new value
	 * @return     SftUsuario The current object (for fluent API support)
	 */
	public function setIdp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->idp !== $v) {
			$this->idp = $v;
			$this->modifiedColumns[] = SftUsuarioPeer::IDP;
		}

		return $this;
	} // setIdp()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->activo !== true) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_sfuser = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->alias = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->activo = ($row[$startcol + 3] !== null) ? (boolean) $row[$startcol + 3] : null;
			$this->fecha_alta = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->fecha_baja = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->created_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->updated_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->id_culturapref = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->id_persona = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->id_organismo = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->idp = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 12; // 12 = SftUsuarioPeer::NUM_COLUMNS - SftUsuarioPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating SftUsuario object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSftPersona !== null && $this->id_persona !== $this->aSftPersona->getId()) {
			$this->aSftPersona = null;
		}
		if ($this->aSftOrganismo !== null && $this->id_organismo !== $this->aSftOrganismo->getId()) {
			$this->aSftOrganismo = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = SftUsuarioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSftPersona = null;
			$this->aSftOrganismo = null;
			$this->collSftAccesos = null;
			$this->lastSftAccesoCriteria = null;

			$this->collSftConfPersonals = null;
			$this->lastSftConfPersonalCriteria = null;

			$this->singleSftControlAcceso = null;

			$this->collSftDireccions = null;
			$this->lastSftDireccionCriteria = null;

			$this->collSftEstadisticaAplicacions = null;
			$this->lastSftEstadisticaAplicacionCriteria = null;

			$this->collSftEmails = null;
			$this->lastSftEmailCriteria = null;

			$this->collSftTelefonos = null;
			$this->lastSftTelefonoCriteria = null;

			$this->collSftUsuAtributoValors = null;
			$this->lastSftUsuAtributoValorCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSftUsuario:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				SftUsuarioPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSftUsuario:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSftUsuario:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(SftUsuarioPeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(SftUsuarioPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSftUsuario:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				SftUsuarioPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSftPersona !== null) {
				if ($this->aSftPersona->isModified() || $this->aSftPersona->isNew()) {
					$affectedRows += $this->aSftPersona->save($con);
				}
				$this->setSftPersona($this->aSftPersona);
			}

			if ($this->aSftOrganismo !== null) {
				if ($this->aSftOrganismo->isModified() || $this->aSftOrganismo->isNew()) {
					$affectedRows += $this->aSftOrganismo->save($con);
				}
				$this->setSftOrganismo($this->aSftOrganismo);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = SftUsuarioPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = SftUsuarioPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += SftUsuarioPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collSftAccesos !== null) {
				foreach ($this->collSftAccesos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSftConfPersonals !== null) {
				foreach ($this->collSftConfPersonals as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->singleSftControlAcceso !== null) {
				if (!$this->singleSftControlAcceso->isDeleted()) {
						$affectedRows += $this->singleSftControlAcceso->save($con);
				}
			}

			if ($this->collSftDireccions !== null) {
				foreach ($this->collSftDireccions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSftEstadisticaAplicacions !== null) {
				foreach ($this->collSftEstadisticaAplicacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSftEmails !== null) {
				foreach ($this->collSftEmails as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSftTelefonos !== null) {
				foreach ($this->collSftTelefonos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSftUsuAtributoValors !== null) {
				foreach ($this->collSftUsuAtributoValors as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSftPersona !== null) {
				if (!$this->aSftPersona->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSftPersona->getValidationFailures());
				}
			}

			if ($this->aSftOrganismo !== null) {
				if (!$this->aSftOrganismo->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSftOrganismo->getValidationFailures());
				}
			}


			if (($retval = SftUsuarioPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collSftAccesos !== null) {
					foreach ($this->collSftAccesos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSftConfPersonals !== null) {
					foreach ($this->collSftConfPersonals as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->singleSftControlAcceso !== null) {
					if (!$this->singleSftControlAcceso->validate($columns)) {
						$failureMap = array_merge($failureMap, $this->singleSftControlAcceso->getValidationFailures());
					}
				}

				if ($this->collSftDireccions !== null) {
					foreach ($this->collSftDireccions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSftEstadisticaAplicacions !== null) {
					foreach ($this->collSftEstadisticaAplicacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSftEmails !== null) {
					foreach ($this->collSftEmails as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSftTelefonos !== null) {
					foreach ($this->collSftTelefonos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSftUsuAtributoValors !== null) {
					foreach ($this->collSftUsuAtributoValors as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SftUsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getIdSfuser();
				break;
			case 2:
				return $this->getAlias();
				break;
			case 3:
				return $this->getActivo();
				break;
			case 4:
				return $this->getFechaAlta();
				break;
			case 5:
				return $this->getFechaBaja();
				break;
			case 6:
				return $this->getCreatedAt();
				break;
			case 7:
				return $this->getUpdatedAt();
				break;
			case 8:
				return $this->getIdCulturapref();
				break;
			case 9:
				return $this->getIdPersona();
				break;
			case 10:
				return $this->getIdOrganismo();
				break;
			case 11:
				return $this->getIdp();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = SftUsuarioPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getIdSfuser(),
			$keys[2] => $this->getAlias(),
			$keys[3] => $this->getActivo(),
			$keys[4] => $this->getFechaAlta(),
			$keys[5] => $this->getFechaBaja(),
			$keys[6] => $this->getCreatedAt(),
			$keys[7] => $this->getUpdatedAt(),
			$keys[8] => $this->getIdCulturapref(),
			$keys[9] => $this->getIdPersona(),
			$keys[10] => $this->getIdOrganismo(),
			$keys[11] => $this->getIdp(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SftUsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setIdSfuser($value);
				break;
			case 2:
				$this->setAlias($value);
				break;
			case 3:
				$this->setActivo($value);
				break;
			case 4:
				$this->setFechaAlta($value);
				break;
			case 5:
				$this->setFechaBaja($value);
				break;
			case 6:
				$this->setCreatedAt($value);
				break;
			case 7:
				$this->setUpdatedAt($value);
				break;
			case 8:
				$this->setIdCulturapref($value);
				break;
			case 9:
				$this->setIdPersona($value);
				break;
			case 10:
				$this->setIdOrganismo($value);
				break;
			case 11:
				$this->setIdp($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = SftUsuarioPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdSfuser($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setAlias($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setActivo($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setFechaAlta($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setFechaBaja($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCreatedAt($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setUpdatedAt($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setIdCulturapref($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setIdPersona($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setIdOrganismo($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setIdp($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);

		if ($this->isColumnModified(SftUsuarioPeer::ID)) $criteria->add(SftUsuarioPeer::ID, $this->id);
		if ($this->isColumnModified(SftUsuarioPeer::ID_SFUSER)) $criteria->add(SftUsuarioPeer::ID_SFUSER, $this->id_sfuser);
		if ($this->isColumnModified(SftUsuarioPeer::ALIAS)) $criteria->add(SftUsuarioPeer::ALIAS, $this->alias);
		if ($this->isColumnModified(SftUsuarioPeer::ACTIVO)) $criteria->add(SftUsuarioPeer::ACTIVO, $this->activo);
		if ($this->isColumnModified(SftUsuarioPeer::FECHA_ALTA)) $criteria->add(SftUsuarioPeer::FECHA_ALTA, $this->fecha_alta);
		if ($this->isColumnModified(SftUsuarioPeer::FECHA_BAJA)) $criteria->add(SftUsuarioPeer::FECHA_BAJA, $this->fecha_baja);
		if ($this->isColumnModified(SftUsuarioPeer::CREATED_AT)) $criteria->add(SftUsuarioPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(SftUsuarioPeer::UPDATED_AT)) $criteria->add(SftUsuarioPeer::UPDATED_AT, $this->updated_at);
		if ($this->isColumnModified(SftUsuarioPeer::ID_CULTURAPREF)) $criteria->add(SftUsuarioPeer::ID_CULTURAPREF, $this->id_culturapref);
		if ($this->isColumnModified(SftUsuarioPeer::ID_PERSONA)) $criteria->add(SftUsuarioPeer::ID_PERSONA, $this->id_persona);
		if ($this->isColumnModified(SftUsuarioPeer::ID_ORGANISMO)) $criteria->add(SftUsuarioPeer::ID_ORGANISMO, $this->id_organismo);
		if ($this->isColumnModified(SftUsuarioPeer::IDP)) $criteria->add(SftUsuarioPeer::IDP, $this->idp);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);

		$criteria->add(SftUsuarioPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of SftUsuario (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdSfuser($this->id_sfuser);

		$copyObj->setAlias($this->alias);

		$copyObj->setActivo($this->activo);

		$copyObj->setFechaAlta($this->fecha_alta);

		$copyObj->setFechaBaja($this->fecha_baja);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);

		$copyObj->setIdCulturapref($this->id_culturapref);

		$copyObj->setIdPersona($this->id_persona);

		$copyObj->setIdOrganismo($this->id_organismo);

		$copyObj->setIdp($this->idp);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getSftAccesos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftAcceso($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSftConfPersonals() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftConfPersonal($relObj->copy($deepCopy));
				}
			}

			$relObj = $this->getSftControlAcceso();
			if ($relObj) {
				$copyObj->setSftControlAcceso($relObj->copy($deepCopy));
			}

			foreach ($this->getSftDireccions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftDireccion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSftEstadisticaAplicacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftEstadisticaAplicacion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSftEmails() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftEmail($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSftTelefonos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftTelefono($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSftUsuAtributoValors() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftUsuAtributoValor($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     SftUsuario Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     SftUsuarioPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new SftUsuarioPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SftPersona object.
	 *
	 * @param      SftPersona $v
	 * @return     SftUsuario The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSftPersona(SftPersona $v = null)
	{
		if ($v === null) {
			$this->setIdPersona(NULL);
		} else {
			$this->setIdPersona($v->getId());
		}

		$this->aSftPersona = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SftPersona object, it will not be re-added.
		if ($v !== null) {
			$v->addSftUsuario($this);
		}

		return $this;
	}


	/**
	 * Get the associated SftPersona object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SftPersona The associated SftPersona object.
	 * @throws     PropelException
	 */
	public function getSftPersona(PropelPDO $con = null)
	{
		if ($this->aSftPersona === null && ($this->id_persona !== null)) {
			$this->aSftPersona = SftPersonaPeer::retrieveByPk($this->id_persona);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSftPersona->addSftUsuarios($this);
			 */
		}
		return $this->aSftPersona;
	}

	/**
	 * Declares an association between this object and a SftOrganismo object.
	 *
	 * @param      SftOrganismo $v
	 * @return     SftUsuario The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSftOrganismo(SftOrganismo $v = null)
	{
		if ($v === null) {
			$this->setIdOrganismo(NULL);
		} else {
			$this->setIdOrganismo($v->getId());
		}

		$this->aSftOrganismo = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SftOrganismo object, it will not be re-added.
		if ($v !== null) {
			$v->addSftUsuario($this);
		}

		return $this;
	}


	/**
	 * Get the associated SftOrganismo object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SftOrganismo The associated SftOrganismo object.
	 * @throws     PropelException
	 */
	public function getSftOrganismo(PropelPDO $con = null)
	{
		if ($this->aSftOrganismo === null && ($this->id_organismo !== null)) {
			$this->aSftOrganismo = SftOrganismoPeer::retrieveByPk($this->id_organismo);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSftOrganismo->addSftUsuarios($this);
			 */
		}
		return $this->aSftOrganismo;
	}

	/**
	 * Clears out the collSftAccesos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftAccesos()
	 */
	public function clearSftAccesos()
	{
		$this->collSftAccesos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftAccesos collection (array).
	 *
	 * By default this just sets the collSftAccesos collection to an empty array (like clearcollSftAccesos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftAccesos()
	{
		$this->collSftAccesos = array();
	}

	/**
	 * Gets an array of SftAcceso objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftAccesos from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftAcceso[]
	 * @throws     PropelException
	 */
	public function getSftAccesos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesos === null) {
			if ($this->isNew()) {
			   $this->collSftAccesos = array();
			} else {

				$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

				SftAccesoPeer::addSelectColumns($criteria);
				$this->collSftAccesos = SftAccesoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

				SftAccesoPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftAccesoCriteria) || !$this->lastSftAccesoCriteria->equals($criteria)) {
					$this->collSftAccesos = SftAccesoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftAccesoCriteria = $criteria;
		return $this->collSftAccesos;
	}

	/**
	 * Returns the number of related SftAcceso objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftAcceso objects.
	 * @throws     PropelException
	 */
	public function countSftAccesos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftAccesos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

				$count = SftAccesoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftAccesoCriteria) || !$this->lastSftAccesoCriteria->equals($criteria)) {
					$count = SftAccesoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftAccesos);
				}
			} else {
				$count = count($this->collSftAccesos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftAcceso object to this object
	 * through the SftAcceso foreign key attribute.
	 *
	 * @param      SftAcceso $l SftAcceso
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftAcceso(SftAcceso $l)
	{
		if ($this->collSftAccesos === null) {
			$this->initSftAccesos();
		}
		if (!in_array($l, $this->collSftAccesos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftAccesos, $l);
			$l->setSftUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftAccesos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftAccesosJoinSftPerfil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesos === null) {
			if ($this->isNew()) {
				$this->collSftAccesos = array();
			} else {

				$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

				$this->collSftAccesos = SftAccesoPeer::doSelectJoinSftPerfil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftAccesoCriteria) || !$this->lastSftAccesoCriteria->equals($criteria)) {
				$this->collSftAccesos = SftAccesoPeer::doSelectJoinSftPerfil($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftAccesoCriteria = $criteria;

		return $this->collSftAccesos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftAccesos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftAccesosJoinSftAccesoRelatedByIdDelega($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesos === null) {
			if ($this->isNew()) {
				$this->collSftAccesos = array();
			} else {

				$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

				$this->collSftAccesos = SftAccesoPeer::doSelectJoinSftAccesoRelatedByIdDelega($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftAccesoPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftAccesoCriteria) || !$this->lastSftAccesoCriteria->equals($criteria)) {
				$this->collSftAccesos = SftAccesoPeer::doSelectJoinSftAccesoRelatedByIdDelega($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftAccesoCriteria = $criteria;

		return $this->collSftAccesos;
	}

	/**
	 * Clears out the collSftConfPersonals collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftConfPersonals()
	 */
	public function clearSftConfPersonals()
	{
		$this->collSftConfPersonals = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftConfPersonals collection (array).
	 *
	 * By default this just sets the collSftConfPersonals collection to an empty array (like clearcollSftConfPersonals());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftConfPersonals()
	{
		$this->collSftConfPersonals = array();
	}

	/**
	 * Gets an array of SftConfPersonal objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftConfPersonals from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftConfPersonal[]
	 * @throws     PropelException
	 */
	public function getSftConfPersonals($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftConfPersonals === null) {
			if ($this->isNew()) {
			   $this->collSftConfPersonals = array();
			} else {

				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				SftConfPersonalPeer::addSelectColumns($criteria);
				$this->collSftConfPersonals = SftConfPersonalPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				SftConfPersonalPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftConfPersonalCriteria) || !$this->lastSftConfPersonalCriteria->equals($criteria)) {
					$this->collSftConfPersonals = SftConfPersonalPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftConfPersonalCriteria = $criteria;
		return $this->collSftConfPersonals;
	}

	/**
	 * Returns the number of related SftConfPersonal objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftConfPersonal objects.
	 * @throws     PropelException
	 */
	public function countSftConfPersonals(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftConfPersonals === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				$count = SftConfPersonalPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftConfPersonalCriteria) || !$this->lastSftConfPersonalCriteria->equals($criteria)) {
					$count = SftConfPersonalPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftConfPersonals);
				}
			} else {
				$count = count($this->collSftConfPersonals);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftConfPersonal object to this object
	 * through the SftConfPersonal foreign key attribute.
	 *
	 * @param      SftConfPersonal $l SftConfPersonal
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftConfPersonal(SftConfPersonal $l)
	{
		if ($this->collSftConfPersonals === null) {
			$this->initSftConfPersonals();
		}
		if (!in_array($l, $this->collSftConfPersonals, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftConfPersonals, $l);
			$l->setSftUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftConfPersonals from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftConfPersonalsJoinSftAplicacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftConfPersonals === null) {
			if ($this->isNew()) {
				$this->collSftConfPersonals = array();
			} else {

				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				$this->collSftConfPersonals = SftConfPersonalPeer::doSelectJoinSftAplicacion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftConfPersonalCriteria) || !$this->lastSftConfPersonalCriteria->equals($criteria)) {
				$this->collSftConfPersonals = SftConfPersonalPeer::doSelectJoinSftAplicacion($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftConfPersonalCriteria = $criteria;

		return $this->collSftConfPersonals;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftConfPersonals from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftConfPersonalsJoinSftPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftConfPersonals === null) {
			if ($this->isNew()) {
				$this->collSftConfPersonals = array();
			} else {

				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				$this->collSftConfPersonals = SftConfPersonalPeer::doSelectJoinSftPeriodo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftConfPersonalCriteria) || !$this->lastSftConfPersonalCriteria->equals($criteria)) {
				$this->collSftConfPersonals = SftConfPersonalPeer::doSelectJoinSftPeriodo($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftConfPersonalCriteria = $criteria;

		return $this->collSftConfPersonals;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftConfPersonals from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftConfPersonalsJoinSftPerfil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftConfPersonals === null) {
			if ($this->isNew()) {
				$this->collSftConfPersonals = array();
			} else {

				$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

				$this->collSftConfPersonals = SftConfPersonalPeer::doSelectJoinSftPerfil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftConfPersonalPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftConfPersonalCriteria) || !$this->lastSftConfPersonalCriteria->equals($criteria)) {
				$this->collSftConfPersonals = SftConfPersonalPeer::doSelectJoinSftPerfil($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftConfPersonalCriteria = $criteria;

		return $this->collSftConfPersonals;
	}

	/**
	 * Gets a single SftControlAcceso object, which is related to this object by a one-to-one relationship.
	 *
	 * @param      PropelPDO $con
	 * @return     SftControlAcceso
	 * @throws     PropelException
	 */
	public function getSftControlAcceso(PropelPDO $con = null)
	{

		if ($this->singleSftControlAcceso === null && !$this->isNew()) {
			$this->singleSftControlAcceso = SftControlAccesoPeer::retrieveByPK($this->id, $con);
		}

		return $this->singleSftControlAcceso;
	}

	/**
	 * Sets a single SftControlAcceso object as related to this object by a one-to-one relationship.
	 *
	 * @param      SftControlAcceso $l SftControlAcceso
	 * @return     SftUsuario The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSftControlAcceso(SftControlAcceso $v)
	{
		$this->singleSftControlAcceso = $v;

		// Make sure that that the passed-in SftControlAcceso isn't already associated with this object
		if ($v->getSftUsuario() === null) {
			$v->setSftUsuario($this);
		}

		return $this;
	}

	/**
	 * Clears out the collSftDireccions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftDireccions()
	 */
	public function clearSftDireccions()
	{
		$this->collSftDireccions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftDireccions collection (array).
	 *
	 * By default this just sets the collSftDireccions collection to an empty array (like clearcollSftDireccions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftDireccions()
	{
		$this->collSftDireccions = array();
	}

	/**
	 * Gets an array of SftDireccion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftDireccions from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftDireccion[]
	 * @throws     PropelException
	 */
	public function getSftDireccions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftDireccions === null) {
			if ($this->isNew()) {
			   $this->collSftDireccions = array();
			} else {

				$criteria->add(SftDireccionPeer::ID_USUARIO, $this->id);

				SftDireccionPeer::addSelectColumns($criteria);
				$this->collSftDireccions = SftDireccionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftDireccionPeer::ID_USUARIO, $this->id);

				SftDireccionPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftDireccionCriteria) || !$this->lastSftDireccionCriteria->equals($criteria)) {
					$this->collSftDireccions = SftDireccionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftDireccionCriteria = $criteria;
		return $this->collSftDireccions;
	}

	/**
	 * Returns the number of related SftDireccion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftDireccion objects.
	 * @throws     PropelException
	 */
	public function countSftDireccions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftDireccions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftDireccionPeer::ID_USUARIO, $this->id);

				$count = SftDireccionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftDireccionPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftDireccionCriteria) || !$this->lastSftDireccionCriteria->equals($criteria)) {
					$count = SftDireccionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftDireccions);
				}
			} else {
				$count = count($this->collSftDireccions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftDireccion object to this object
	 * through the SftDireccion foreign key attribute.
	 *
	 * @param      SftDireccion $l SftDireccion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftDireccion(SftDireccion $l)
	{
		if ($this->collSftDireccions === null) {
			$this->initSftDireccions();
		}
		if (!in_array($l, $this->collSftDireccions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftDireccions, $l);
			$l->setSftUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftDireccions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftDireccionsJoinSftTipoDireccion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftDireccions === null) {
			if ($this->isNew()) {
				$this->collSftDireccions = array();
			} else {

				$criteria->add(SftDireccionPeer::ID_USUARIO, $this->id);

				$this->collSftDireccions = SftDireccionPeer::doSelectJoinSftTipoDireccion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftDireccionPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftDireccionCriteria) || !$this->lastSftDireccionCriteria->equals($criteria)) {
				$this->collSftDireccions = SftDireccionPeer::doSelectJoinSftTipoDireccion($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftDireccionCriteria = $criteria;

		return $this->collSftDireccions;
	}

	/**
	 * Clears out the collSftEstadisticaAplicacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftEstadisticaAplicacions()
	 */
	public function clearSftEstadisticaAplicacions()
	{
		$this->collSftEstadisticaAplicacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftEstadisticaAplicacions collection (array).
	 *
	 * By default this just sets the collSftEstadisticaAplicacions collection to an empty array (like clearcollSftEstadisticaAplicacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftEstadisticaAplicacions()
	{
		$this->collSftEstadisticaAplicacions = array();
	}

	/**
	 * Gets an array of SftEstadisticaAplicacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftEstadisticaAplicacions from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftEstadisticaAplicacion[]
	 * @throws     PropelException
	 */
	public function getSftEstadisticaAplicacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftEstadisticaAplicacions === null) {
			if ($this->isNew()) {
			   $this->collSftEstadisticaAplicacions = array();
			} else {

				$criteria->add(SftEstadisticaAplicacionPeer::ID_USUARIO, $this->id);

				SftEstadisticaAplicacionPeer::addSelectColumns($criteria);
				$this->collSftEstadisticaAplicacions = SftEstadisticaAplicacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftEstadisticaAplicacionPeer::ID_USUARIO, $this->id);

				SftEstadisticaAplicacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftEstadisticaAplicacionCriteria) || !$this->lastSftEstadisticaAplicacionCriteria->equals($criteria)) {
					$this->collSftEstadisticaAplicacions = SftEstadisticaAplicacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftEstadisticaAplicacionCriteria = $criteria;
		return $this->collSftEstadisticaAplicacions;
	}

	/**
	 * Returns the number of related SftEstadisticaAplicacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftEstadisticaAplicacion objects.
	 * @throws     PropelException
	 */
	public function countSftEstadisticaAplicacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftEstadisticaAplicacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftEstadisticaAplicacionPeer::ID_USUARIO, $this->id);

				$count = SftEstadisticaAplicacionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftEstadisticaAplicacionPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftEstadisticaAplicacionCriteria) || !$this->lastSftEstadisticaAplicacionCriteria->equals($criteria)) {
					$count = SftEstadisticaAplicacionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftEstadisticaAplicacions);
				}
			} else {
				$count = count($this->collSftEstadisticaAplicacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftEstadisticaAplicacion object to this object
	 * through the SftEstadisticaAplicacion foreign key attribute.
	 *
	 * @param      SftEstadisticaAplicacion $l SftEstadisticaAplicacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftEstadisticaAplicacion(SftEstadisticaAplicacion $l)
	{
		if ($this->collSftEstadisticaAplicacions === null) {
			$this->initSftEstadisticaAplicacions();
		}
		if (!in_array($l, $this->collSftEstadisticaAplicacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftEstadisticaAplicacions, $l);
			$l->setSftUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftEstadisticaAplicacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftEstadisticaAplicacionsJoinSftAplicacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftEstadisticaAplicacions === null) {
			if ($this->isNew()) {
				$this->collSftEstadisticaAplicacions = array();
			} else {

				$criteria->add(SftEstadisticaAplicacionPeer::ID_USUARIO, $this->id);

				$this->collSftEstadisticaAplicacions = SftEstadisticaAplicacionPeer::doSelectJoinSftAplicacion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftEstadisticaAplicacionPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftEstadisticaAplicacionCriteria) || !$this->lastSftEstadisticaAplicacionCriteria->equals($criteria)) {
				$this->collSftEstadisticaAplicacions = SftEstadisticaAplicacionPeer::doSelectJoinSftAplicacion($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftEstadisticaAplicacionCriteria = $criteria;

		return $this->collSftEstadisticaAplicacions;
	}

	/**
	 * Clears out the collSftEmails collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftEmails()
	 */
	public function clearSftEmails()
	{
		$this->collSftEmails = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftEmails collection (array).
	 *
	 * By default this just sets the collSftEmails collection to an empty array (like clearcollSftEmails());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftEmails()
	{
		$this->collSftEmails = array();
	}

	/**
	 * Gets an array of SftEmail objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftEmails from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftEmail[]
	 * @throws     PropelException
	 */
	public function getSftEmails($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftEmails === null) {
			if ($this->isNew()) {
			   $this->collSftEmails = array();
			} else {

				$criteria->add(SftEmailPeer::ID_USUARIO, $this->id);

				SftEmailPeer::addSelectColumns($criteria);
				$this->collSftEmails = SftEmailPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftEmailPeer::ID_USUARIO, $this->id);

				SftEmailPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftEmailCriteria) || !$this->lastSftEmailCriteria->equals($criteria)) {
					$this->collSftEmails = SftEmailPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftEmailCriteria = $criteria;
		return $this->collSftEmails;
	}

	/**
	 * Returns the number of related SftEmail objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftEmail objects.
	 * @throws     PropelException
	 */
	public function countSftEmails(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftEmails === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftEmailPeer::ID_USUARIO, $this->id);

				$count = SftEmailPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftEmailPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftEmailCriteria) || !$this->lastSftEmailCriteria->equals($criteria)) {
					$count = SftEmailPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftEmails);
				}
			} else {
				$count = count($this->collSftEmails);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftEmail object to this object
	 * through the SftEmail foreign key attribute.
	 *
	 * @param      SftEmail $l SftEmail
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftEmail(SftEmail $l)
	{
		if ($this->collSftEmails === null) {
			$this->initSftEmails();
		}
		if (!in_array($l, $this->collSftEmails, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftEmails, $l);
			$l->setSftUsuario($this);
		}
	}

	/**
	 * Clears out the collSftTelefonos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftTelefonos()
	 */
	public function clearSftTelefonos()
	{
		$this->collSftTelefonos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftTelefonos collection (array).
	 *
	 * By default this just sets the collSftTelefonos collection to an empty array (like clearcollSftTelefonos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftTelefonos()
	{
		$this->collSftTelefonos = array();
	}

	/**
	 * Gets an array of SftTelefono objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftTelefonos from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftTelefono[]
	 * @throws     PropelException
	 */
	public function getSftTelefonos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftTelefonos === null) {
			if ($this->isNew()) {
			   $this->collSftTelefonos = array();
			} else {

				$criteria->add(SftTelefonoPeer::ID_USUARIO, $this->id);

				SftTelefonoPeer::addSelectColumns($criteria);
				$this->collSftTelefonos = SftTelefonoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftTelefonoPeer::ID_USUARIO, $this->id);

				SftTelefonoPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftTelefonoCriteria) || !$this->lastSftTelefonoCriteria->equals($criteria)) {
					$this->collSftTelefonos = SftTelefonoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftTelefonoCriteria = $criteria;
		return $this->collSftTelefonos;
	}

	/**
	 * Returns the number of related SftTelefono objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftTelefono objects.
	 * @throws     PropelException
	 */
	public function countSftTelefonos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftTelefonos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftTelefonoPeer::ID_USUARIO, $this->id);

				$count = SftTelefonoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftTelefonoPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftTelefonoCriteria) || !$this->lastSftTelefonoCriteria->equals($criteria)) {
					$count = SftTelefonoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftTelefonos);
				}
			} else {
				$count = count($this->collSftTelefonos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftTelefono object to this object
	 * through the SftTelefono foreign key attribute.
	 *
	 * @param      SftTelefono $l SftTelefono
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftTelefono(SftTelefono $l)
	{
		if ($this->collSftTelefonos === null) {
			$this->initSftTelefonos();
		}
		if (!in_array($l, $this->collSftTelefonos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftTelefonos, $l);
			$l->setSftUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftTelefonos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftTelefonosJoinSftTipoTelefono($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftTelefonos === null) {
			if ($this->isNew()) {
				$this->collSftTelefonos = array();
			} else {

				$criteria->add(SftTelefonoPeer::ID_USUARIO, $this->id);

				$this->collSftTelefonos = SftTelefonoPeer::doSelectJoinSftTipoTelefono($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftTelefonoPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftTelefonoCriteria) || !$this->lastSftTelefonoCriteria->equals($criteria)) {
				$this->collSftTelefonos = SftTelefonoPeer::doSelectJoinSftTipoTelefono($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftTelefonoCriteria = $criteria;

		return $this->collSftTelefonos;
	}

	/**
	 * Clears out the collSftUsuAtributoValors collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftUsuAtributoValors()
	 */
	public function clearSftUsuAtributoValors()
	{
		$this->collSftUsuAtributoValors = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftUsuAtributoValors collection (array).
	 *
	 * By default this just sets the collSftUsuAtributoValors collection to an empty array (like clearcollSftUsuAtributoValors());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftUsuAtributoValors()
	{
		$this->collSftUsuAtributoValors = array();
	}

	/**
	 * Gets an array of SftUsuAtributoValor objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftUsuario has previously been saved, it will retrieve
	 * related SftUsuAtributoValors from storage. If this SftUsuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftUsuAtributoValor[]
	 * @throws     PropelException
	 */
	public function getSftUsuAtributoValors($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftUsuAtributoValors === null) {
			if ($this->isNew()) {
			   $this->collSftUsuAtributoValors = array();
			} else {

				$criteria->add(SftUsuAtributoValorPeer::ID_USUARIO, $this->id);

				SftUsuAtributoValorPeer::addSelectColumns($criteria);
				$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftUsuAtributoValorPeer::ID_USUARIO, $this->id);

				SftUsuAtributoValorPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftUsuAtributoValorCriteria) || !$this->lastSftUsuAtributoValorCriteria->equals($criteria)) {
					$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftUsuAtributoValorCriteria = $criteria;
		return $this->collSftUsuAtributoValors;
	}

	/**
	 * Returns the number of related SftUsuAtributoValor objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftUsuAtributoValor objects.
	 * @throws     PropelException
	 */
	public function countSftUsuAtributoValors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftUsuAtributoValors === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftUsuAtributoValorPeer::ID_USUARIO, $this->id);

				$count = SftUsuAtributoValorPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftUsuAtributoValorPeer::ID_USUARIO, $this->id);

				if (!isset($this->lastSftUsuAtributoValorCriteria) || !$this->lastSftUsuAtributoValorCriteria->equals($criteria)) {
					$count = SftUsuAtributoValorPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftUsuAtributoValors);
				}
			} else {
				$count = count($this->collSftUsuAtributoValors);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftUsuAtributoValor object to this object
	 * through the SftUsuAtributoValor foreign key attribute.
	 *
	 * @param      SftUsuAtributoValor $l SftUsuAtributoValor
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftUsuAtributoValor(SftUsuAtributoValor $l)
	{
		if ($this->collSftUsuAtributoValors === null) {
			$this->initSftUsuAtributoValors();
		}
		if (!in_array($l, $this->collSftUsuAtributoValors, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftUsuAtributoValors, $l);
			$l->setSftUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftUsuario is new, it will return
	 * an empty collection; or if this SftUsuario has previously
	 * been saved, it will retrieve related SftUsuAtributoValors from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftUsuario.
	 */
	public function getSftUsuAtributoValorsJoinSftUsuAtributo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftUsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftUsuAtributoValors === null) {
			if ($this->isNew()) {
				$this->collSftUsuAtributoValors = array();
			} else {

				$criteria->add(SftUsuAtributoValorPeer::ID_USUARIO, $this->id);

				$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelectJoinSftUsuAtributo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftUsuAtributoValorPeer::ID_USUARIO, $this->id);

			if (!isset($this->lastSftUsuAtributoValorCriteria) || !$this->lastSftUsuAtributoValorCriteria->equals($criteria)) {
				$this->collSftUsuAtributoValors = SftUsuAtributoValorPeer::doSelectJoinSftUsuAtributo($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftUsuAtributoValorCriteria = $criteria;

		return $this->collSftUsuAtributoValors;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collSftAccesos) {
				foreach ((array) $this->collSftAccesos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSftConfPersonals) {
				foreach ((array) $this->collSftConfPersonals as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->singleSftControlAcceso) {
				$this->singleSftControlAcceso->clearAllReferences($deep);
			}
			if ($this->collSftDireccions) {
				foreach ((array) $this->collSftDireccions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSftEstadisticaAplicacions) {
				foreach ((array) $this->collSftEstadisticaAplicacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSftEmails) {
				foreach ((array) $this->collSftEmails as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSftTelefonos) {
				foreach ((array) $this->collSftTelefonos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSftUsuAtributoValors) {
				foreach ((array) $this->collSftUsuAtributoValors as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collSftAccesos = null;
		$this->collSftConfPersonals = null;
		$this->singleSftControlAcceso = null;
		$this->collSftDireccions = null;
		$this->collSftEstadisticaAplicacions = null;
		$this->collSftEmails = null;
		$this->collSftTelefonos = null;
		$this->collSftUsuAtributoValors = null;
			$this->aSftPersona = null;
			$this->aSftOrganismo = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseSftUsuario:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseSftUsuario::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseSftUsuario
