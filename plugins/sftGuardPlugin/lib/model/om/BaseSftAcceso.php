<?php

/**
 * Base class that represents a row from the 'sft_accesos' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Mon Sep 24 21:13:06 2012
 *
 * @package    plugins.sftGuardPlugin.lib.model.om
 */
abstract class BaseSftAcceso extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        SftAccesoPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the id_usuario field.
	 * @var        int
	 */
	protected $id_usuario;

	/**
	 * The value for the id_perfil field.
	 * @var        int
	 */
	protected $id_perfil;

	/**
	 * The value for the delega field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $delega;

	/**
	 * The value for the id_delega field.
	 * @var        int
	 */
	protected $id_delega;

	/**
	 * The value for the esinicial field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $esinicial;

	/**
	 * @var        SftUsuario
	 */
	protected $aSftUsuario;

	/**
	 * @var        SftPerfil
	 */
	protected $aSftPerfil;

	/**
	 * @var        SftAcceso
	 */
	protected $aSftAccesoRelatedByIdDelega;

	/**
	 * @var        array SftAcceso[] Collection to store aggregation of SftAcceso objects.
	 */
	protected $collSftAccesosRelatedByIdDelega;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftAccesosRelatedByIdDelega.
	 */
	private $lastSftAccesoRelatedByIdDelegaCriteria = null;

	/**
	 * @var        array SftAccesoAmbito[] Collection to store aggregation of SftAccesoAmbito objects.
	 */
	protected $collSftAccesoAmbitos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSftAccesoAmbitos.
	 */
	private $lastSftAccesoAmbitoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'SftAccesoPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->delega = 0;
		$this->esinicial = 0;
	}

	/**
	 * Initializes internal state of BaseSftAcceso object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [id_usuario] column value.
	 * 
	 * @return     int
	 */
	public function getIdUsuario()
	{
		return $this->id_usuario;
	}

	/**
	 * Get the [id_perfil] column value.
	 * 
	 * @return     int
	 */
	public function getIdPerfil()
	{
		return $this->id_perfil;
	}

	/**
	 * Get the [delega] column value.
	 * 
	 * @return     int
	 */
	public function getDelega()
	{
		return $this->delega;
	}

	/**
	 * Get the [id_delega] column value.
	 * 
	 * @return     int
	 */
	public function getIdDelega()
	{
		return $this->id_delega;
	}

	/**
	 * Get the [esinicial] column value.
	 * 
	 * @return     int
	 */
	public function getEsinicial()
	{
		return $this->esinicial;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     SftAcceso The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = SftAccesoPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [id_usuario] column.
	 * 
	 * @param      int $v new value
	 * @return     SftAcceso The current object (for fluent API support)
	 */
	public function setIdUsuario($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_usuario !== $v) {
			$this->id_usuario = $v;
			$this->modifiedColumns[] = SftAccesoPeer::ID_USUARIO;
		}

		if ($this->aSftUsuario !== null && $this->aSftUsuario->getId() !== $v) {
			$this->aSftUsuario = null;
		}

		return $this;
	} // setIdUsuario()

	/**
	 * Set the value of [id_perfil] column.
	 * 
	 * @param      int $v new value
	 * @return     SftAcceso The current object (for fluent API support)
	 */
	public function setIdPerfil($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_perfil !== $v) {
			$this->id_perfil = $v;
			$this->modifiedColumns[] = SftAccesoPeer::ID_PERFIL;
		}

		if ($this->aSftPerfil !== null && $this->aSftPerfil->getId() !== $v) {
			$this->aSftPerfil = null;
		}

		return $this;
	} // setIdPerfil()

	/**
	 * Set the value of [delega] column.
	 * 
	 * @param      int $v new value
	 * @return     SftAcceso The current object (for fluent API support)
	 */
	public function setDelega($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->delega !== $v || $this->isNew()) {
			$this->delega = $v;
			$this->modifiedColumns[] = SftAccesoPeer::DELEGA;
		}

		return $this;
	} // setDelega()

	/**
	 * Set the value of [id_delega] column.
	 * 
	 * @param      int $v new value
	 * @return     SftAcceso The current object (for fluent API support)
	 */
	public function setIdDelega($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_delega !== $v) {
			$this->id_delega = $v;
			$this->modifiedColumns[] = SftAccesoPeer::ID_DELEGA;
		}

		if ($this->aSftAccesoRelatedByIdDelega !== null && $this->aSftAccesoRelatedByIdDelega->getId() !== $v) {
			$this->aSftAccesoRelatedByIdDelega = null;
		}

		return $this;
	} // setIdDelega()

	/**
	 * Set the value of [esinicial] column.
	 * 
	 * @param      int $v new value
	 * @return     SftAcceso The current object (for fluent API support)
	 */
	public function setEsinicial($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->esinicial !== $v || $this->isNew()) {
			$this->esinicial = $v;
			$this->modifiedColumns[] = SftAccesoPeer::ESINICIAL;
		}

		return $this;
	} // setEsinicial()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->delega !== 0) {
				return false;
			}

			if ($this->esinicial !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_usuario = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->id_perfil = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->delega = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->id_delega = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->esinicial = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 6; // 6 = SftAccesoPeer::NUM_COLUMNS - SftAccesoPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating SftAcceso object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSftUsuario !== null && $this->id_usuario !== $this->aSftUsuario->getId()) {
			$this->aSftUsuario = null;
		}
		if ($this->aSftPerfil !== null && $this->id_perfil !== $this->aSftPerfil->getId()) {
			$this->aSftPerfil = null;
		}
		if ($this->aSftAccesoRelatedByIdDelega !== null && $this->id_delega !== $this->aSftAccesoRelatedByIdDelega->getId()) {
			$this->aSftAccesoRelatedByIdDelega = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftAccesoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = SftAccesoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSftUsuario = null;
			$this->aSftPerfil = null;
			$this->aSftAccesoRelatedByIdDelega = null;
			$this->collSftAccesosRelatedByIdDelega = null;
			$this->lastSftAccesoRelatedByIdDelegaCriteria = null;

			$this->collSftAccesoAmbitos = null;
			$this->lastSftAccesoAmbitoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftAccesoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSftAcceso:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				SftAccesoPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSftAcceso:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(SftAccesoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseSftAcceso:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseSftAcceso:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				SftAccesoPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSftUsuario !== null) {
				if ($this->aSftUsuario->isModified() || $this->aSftUsuario->isNew()) {
					$affectedRows += $this->aSftUsuario->save($con);
				}
				$this->setSftUsuario($this->aSftUsuario);
			}

			if ($this->aSftPerfil !== null) {
				if ($this->aSftPerfil->isModified() || $this->aSftPerfil->isNew()) {
					$affectedRows += $this->aSftPerfil->save($con);
				}
				$this->setSftPerfil($this->aSftPerfil);
			}

			if ($this->aSftAccesoRelatedByIdDelega !== null) {
				if ($this->aSftAccesoRelatedByIdDelega->isModified() || $this->aSftAccesoRelatedByIdDelega->isNew()) {
					$affectedRows += $this->aSftAccesoRelatedByIdDelega->save($con);
				}
				$this->setSftAccesoRelatedByIdDelega($this->aSftAccesoRelatedByIdDelega);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = SftAccesoPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = SftAccesoPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += SftAccesoPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collSftAccesosRelatedByIdDelega !== null) {
				foreach ($this->collSftAccesosRelatedByIdDelega as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSftAccesoAmbitos !== null) {
				foreach ($this->collSftAccesoAmbitos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSftUsuario !== null) {
				if (!$this->aSftUsuario->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSftUsuario->getValidationFailures());
				}
			}

			if ($this->aSftPerfil !== null) {
				if (!$this->aSftPerfil->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSftPerfil->getValidationFailures());
				}
			}

			if ($this->aSftAccesoRelatedByIdDelega !== null) {
				if (!$this->aSftAccesoRelatedByIdDelega->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSftAccesoRelatedByIdDelega->getValidationFailures());
				}
			}


			if (($retval = SftAccesoPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collSftAccesosRelatedByIdDelega !== null) {
					foreach ($this->collSftAccesosRelatedByIdDelega as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSftAccesoAmbitos !== null) {
					foreach ($this->collSftAccesoAmbitos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SftAccesoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getIdUsuario();
				break;
			case 2:
				return $this->getIdPerfil();
				break;
			case 3:
				return $this->getDelega();
				break;
			case 4:
				return $this->getIdDelega();
				break;
			case 5:
				return $this->getEsinicial();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = SftAccesoPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getIdUsuario(),
			$keys[2] => $this->getIdPerfil(),
			$keys[3] => $this->getDelega(),
			$keys[4] => $this->getIdDelega(),
			$keys[5] => $this->getEsinicial(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = SftAccesoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setIdUsuario($value);
				break;
			case 2:
				$this->setIdPerfil($value);
				break;
			case 3:
				$this->setDelega($value);
				break;
			case 4:
				$this->setIdDelega($value);
				break;
			case 5:
				$this->setEsinicial($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = SftAccesoPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdUsuario($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setIdPerfil($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setDelega($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setIdDelega($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setEsinicial($arr[$keys[5]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);

		if ($this->isColumnModified(SftAccesoPeer::ID)) $criteria->add(SftAccesoPeer::ID, $this->id);
		if ($this->isColumnModified(SftAccesoPeer::ID_USUARIO)) $criteria->add(SftAccesoPeer::ID_USUARIO, $this->id_usuario);
		if ($this->isColumnModified(SftAccesoPeer::ID_PERFIL)) $criteria->add(SftAccesoPeer::ID_PERFIL, $this->id_perfil);
		if ($this->isColumnModified(SftAccesoPeer::DELEGA)) $criteria->add(SftAccesoPeer::DELEGA, $this->delega);
		if ($this->isColumnModified(SftAccesoPeer::ID_DELEGA)) $criteria->add(SftAccesoPeer::ID_DELEGA, $this->id_delega);
		if ($this->isColumnModified(SftAccesoPeer::ESINICIAL)) $criteria->add(SftAccesoPeer::ESINICIAL, $this->esinicial);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);

		$criteria->add(SftAccesoPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of SftAcceso (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdUsuario($this->id_usuario);

		$copyObj->setIdPerfil($this->id_perfil);

		$copyObj->setDelega($this->delega);

		$copyObj->setIdDelega($this->id_delega);

		$copyObj->setEsinicial($this->esinicial);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getSftAccesosRelatedByIdDelega() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftAccesoRelatedByIdDelega($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSftAccesoAmbitos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSftAccesoAmbito($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     SftAcceso Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     SftAccesoPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new SftAccesoPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SftUsuario object.
	 *
	 * @param      SftUsuario $v
	 * @return     SftAcceso The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSftUsuario(SftUsuario $v = null)
	{
		if ($v === null) {
			$this->setIdUsuario(NULL);
		} else {
			$this->setIdUsuario($v->getId());
		}

		$this->aSftUsuario = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SftUsuario object, it will not be re-added.
		if ($v !== null) {
			$v->addSftAcceso($this);
		}

		return $this;
	}


	/**
	 * Get the associated SftUsuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SftUsuario The associated SftUsuario object.
	 * @throws     PropelException
	 */
	public function getSftUsuario(PropelPDO $con = null)
	{
		if ($this->aSftUsuario === null && ($this->id_usuario !== null)) {
			$this->aSftUsuario = SftUsuarioPeer::retrieveByPk($this->id_usuario);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSftUsuario->addSftAccesos($this);
			 */
		}
		return $this->aSftUsuario;
	}

	/**
	 * Declares an association between this object and a SftPerfil object.
	 *
	 * @param      SftPerfil $v
	 * @return     SftAcceso The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSftPerfil(SftPerfil $v = null)
	{
		if ($v === null) {
			$this->setIdPerfil(NULL);
		} else {
			$this->setIdPerfil($v->getId());
		}

		$this->aSftPerfil = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SftPerfil object, it will not be re-added.
		if ($v !== null) {
			$v->addSftAcceso($this);
		}

		return $this;
	}


	/**
	 * Get the associated SftPerfil object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SftPerfil The associated SftPerfil object.
	 * @throws     PropelException
	 */
	public function getSftPerfil(PropelPDO $con = null)
	{
		if ($this->aSftPerfil === null && ($this->id_perfil !== null)) {
			$this->aSftPerfil = SftPerfilPeer::retrieveByPk($this->id_perfil);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSftPerfil->addSftAccesos($this);
			 */
		}
		return $this->aSftPerfil;
	}

	/**
	 * Declares an association between this object and a SftAcceso object.
	 *
	 * @param      SftAcceso $v
	 * @return     SftAcceso The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSftAccesoRelatedByIdDelega(SftAcceso $v = null)
	{
		if ($v === null) {
			$this->setIdDelega(NULL);
		} else {
			$this->setIdDelega($v->getId());
		}

		$this->aSftAccesoRelatedByIdDelega = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SftAcceso object, it will not be re-added.
		if ($v !== null) {
			$v->addSftAccesoRelatedByIdDelega($this);
		}

		return $this;
	}


	/**
	 * Get the associated SftAcceso object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SftAcceso The associated SftAcceso object.
	 * @throws     PropelException
	 */
	public function getSftAccesoRelatedByIdDelega(PropelPDO $con = null)
	{
		if ($this->aSftAccesoRelatedByIdDelega === null && ($this->id_delega !== null)) {
			$this->aSftAccesoRelatedByIdDelega = SftAccesoPeer::retrieveByPk($this->id_delega);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSftAccesoRelatedByIdDelega->addSftAccesosRelatedByIdDelega($this);
			 */
		}
		return $this->aSftAccesoRelatedByIdDelega;
	}

	/**
	 * Clears out the collSftAccesosRelatedByIdDelega collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftAccesosRelatedByIdDelega()
	 */
	public function clearSftAccesosRelatedByIdDelega()
	{
		$this->collSftAccesosRelatedByIdDelega = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftAccesosRelatedByIdDelega collection (array).
	 *
	 * By default this just sets the collSftAccesosRelatedByIdDelega collection to an empty array (like clearcollSftAccesosRelatedByIdDelega());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftAccesosRelatedByIdDelega()
	{
		$this->collSftAccesosRelatedByIdDelega = array();
	}

	/**
	 * Gets an array of SftAcceso objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftAcceso has previously been saved, it will retrieve
	 * related SftAccesosRelatedByIdDelega from storage. If this SftAcceso is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftAcceso[]
	 * @throws     PropelException
	 */
	public function getSftAccesosRelatedByIdDelega($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesosRelatedByIdDelega === null) {
			if ($this->isNew()) {
			   $this->collSftAccesosRelatedByIdDelega = array();
			} else {

				$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

				SftAccesoPeer::addSelectColumns($criteria);
				$this->collSftAccesosRelatedByIdDelega = SftAccesoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

				SftAccesoPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftAccesoRelatedByIdDelegaCriteria) || !$this->lastSftAccesoRelatedByIdDelegaCriteria->equals($criteria)) {
					$this->collSftAccesosRelatedByIdDelega = SftAccesoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftAccesoRelatedByIdDelegaCriteria = $criteria;
		return $this->collSftAccesosRelatedByIdDelega;
	}

	/**
	 * Returns the number of related SftAcceso objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftAcceso objects.
	 * @throws     PropelException
	 */
	public function countSftAccesosRelatedByIdDelega(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftAccesosRelatedByIdDelega === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

				$count = SftAccesoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

				if (!isset($this->lastSftAccesoRelatedByIdDelegaCriteria) || !$this->lastSftAccesoRelatedByIdDelegaCriteria->equals($criteria)) {
					$count = SftAccesoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftAccesosRelatedByIdDelega);
				}
			} else {
				$count = count($this->collSftAccesosRelatedByIdDelega);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftAcceso object to this object
	 * through the SftAcceso foreign key attribute.
	 *
	 * @param      SftAcceso $l SftAcceso
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftAccesoRelatedByIdDelega(SftAcceso $l)
	{
		if ($this->collSftAccesosRelatedByIdDelega === null) {
			$this->initSftAccesosRelatedByIdDelega();
		}
		if (!in_array($l, $this->collSftAccesosRelatedByIdDelega, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftAccesosRelatedByIdDelega, $l);
			$l->setSftAccesoRelatedByIdDelega($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftAcceso is new, it will return
	 * an empty collection; or if this SftAcceso has previously
	 * been saved, it will retrieve related SftAccesosRelatedByIdDelega from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftAcceso.
	 */
	public function getSftAccesosRelatedByIdDelegaJoinSftUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesosRelatedByIdDelega === null) {
			if ($this->isNew()) {
				$this->collSftAccesosRelatedByIdDelega = array();
			} else {

				$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

				$this->collSftAccesosRelatedByIdDelega = SftAccesoPeer::doSelectJoinSftUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

			if (!isset($this->lastSftAccesoRelatedByIdDelegaCriteria) || !$this->lastSftAccesoRelatedByIdDelegaCriteria->equals($criteria)) {
				$this->collSftAccesosRelatedByIdDelega = SftAccesoPeer::doSelectJoinSftUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftAccesoRelatedByIdDelegaCriteria = $criteria;

		return $this->collSftAccesosRelatedByIdDelega;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftAcceso is new, it will return
	 * an empty collection; or if this SftAcceso has previously
	 * been saved, it will retrieve related SftAccesosRelatedByIdDelega from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftAcceso.
	 */
	public function getSftAccesosRelatedByIdDelegaJoinSftPerfil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesosRelatedByIdDelega === null) {
			if ($this->isNew()) {
				$this->collSftAccesosRelatedByIdDelega = array();
			} else {

				$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

				$this->collSftAccesosRelatedByIdDelega = SftAccesoPeer::doSelectJoinSftPerfil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftAccesoPeer::ID_DELEGA, $this->id);

			if (!isset($this->lastSftAccesoRelatedByIdDelegaCriteria) || !$this->lastSftAccesoRelatedByIdDelegaCriteria->equals($criteria)) {
				$this->collSftAccesosRelatedByIdDelega = SftAccesoPeer::doSelectJoinSftPerfil($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftAccesoRelatedByIdDelegaCriteria = $criteria;

		return $this->collSftAccesosRelatedByIdDelega;
	}

	/**
	 * Clears out the collSftAccesoAmbitos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSftAccesoAmbitos()
	 */
	public function clearSftAccesoAmbitos()
	{
		$this->collSftAccesoAmbitos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSftAccesoAmbitos collection (array).
	 *
	 * By default this just sets the collSftAccesoAmbitos collection to an empty array (like clearcollSftAccesoAmbitos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSftAccesoAmbitos()
	{
		$this->collSftAccesoAmbitos = array();
	}

	/**
	 * Gets an array of SftAccesoAmbito objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this SftAcceso has previously been saved, it will retrieve
	 * related SftAccesoAmbitos from storage. If this SftAcceso is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SftAccesoAmbito[]
	 * @throws     PropelException
	 */
	public function getSftAccesoAmbitos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesoAmbitos === null) {
			if ($this->isNew()) {
			   $this->collSftAccesoAmbitos = array();
			} else {

				$criteria->add(SftAccesoAmbitoPeer::ID_ACCESO, $this->id);

				SftAccesoAmbitoPeer::addSelectColumns($criteria);
				$this->collSftAccesoAmbitos = SftAccesoAmbitoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SftAccesoAmbitoPeer::ID_ACCESO, $this->id);

				SftAccesoAmbitoPeer::addSelectColumns($criteria);
				if (!isset($this->lastSftAccesoAmbitoCriteria) || !$this->lastSftAccesoAmbitoCriteria->equals($criteria)) {
					$this->collSftAccesoAmbitos = SftAccesoAmbitoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSftAccesoAmbitoCriteria = $criteria;
		return $this->collSftAccesoAmbitos;
	}

	/**
	 * Returns the number of related SftAccesoAmbito objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SftAccesoAmbito objects.
	 * @throws     PropelException
	 */
	public function countSftAccesoAmbitos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSftAccesoAmbitos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SftAccesoAmbitoPeer::ID_ACCESO, $this->id);

				$count = SftAccesoAmbitoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SftAccesoAmbitoPeer::ID_ACCESO, $this->id);

				if (!isset($this->lastSftAccesoAmbitoCriteria) || !$this->lastSftAccesoAmbitoCriteria->equals($criteria)) {
					$count = SftAccesoAmbitoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSftAccesoAmbitos);
				}
			} else {
				$count = count($this->collSftAccesoAmbitos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SftAccesoAmbito object to this object
	 * through the SftAccesoAmbito foreign key attribute.
	 *
	 * @param      SftAccesoAmbito $l SftAccesoAmbito
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSftAccesoAmbito(SftAccesoAmbito $l)
	{
		if ($this->collSftAccesoAmbitos === null) {
			$this->initSftAccesoAmbitos();
		}
		if (!in_array($l, $this->collSftAccesoAmbitos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSftAccesoAmbitos, $l);
			$l->setSftAcceso($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this SftAcceso is new, it will return
	 * an empty collection; or if this SftAcceso has previously
	 * been saved, it will retrieve related SftAccesoAmbitos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in SftAcceso.
	 */
	public function getSftAccesoAmbitosJoinSftAmbito($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(SftAccesoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSftAccesoAmbitos === null) {
			if ($this->isNew()) {
				$this->collSftAccesoAmbitos = array();
			} else {

				$criteria->add(SftAccesoAmbitoPeer::ID_ACCESO, $this->id);

				$this->collSftAccesoAmbitos = SftAccesoAmbitoPeer::doSelectJoinSftAmbito($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SftAccesoAmbitoPeer::ID_ACCESO, $this->id);

			if (!isset($this->lastSftAccesoAmbitoCriteria) || !$this->lastSftAccesoAmbitoCriteria->equals($criteria)) {
				$this->collSftAccesoAmbitos = SftAccesoAmbitoPeer::doSelectJoinSftAmbito($criteria, $con, $join_behavior);
			}
		}
		$this->lastSftAccesoAmbitoCriteria = $criteria;

		return $this->collSftAccesoAmbitos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collSftAccesosRelatedByIdDelega) {
				foreach ((array) $this->collSftAccesosRelatedByIdDelega as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSftAccesoAmbitos) {
				foreach ((array) $this->collSftAccesoAmbitos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collSftAccesosRelatedByIdDelega = null;
		$this->collSftAccesoAmbitos = null;
			$this->aSftUsuario = null;
			$this->aSftPerfil = null;
			$this->aSftAccesoRelatedByIdDelega = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseSftAcceso:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseSftAcceso::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseSftAcceso
